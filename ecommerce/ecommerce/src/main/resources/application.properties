# Server Port
server.port=8080

# MySQL Database Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db14?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password= Sarthak@2521

# JPA/Hibernate Settings
# 'update' automatically modifies the database table to match your Java code.
spring.jpa.hibernate.ddl-auto=update
# Shows the SQL queries in the console, which is helpful for debugging.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect